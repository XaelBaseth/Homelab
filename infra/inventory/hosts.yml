---
all:
  hosts:
    beelink:
      ansible_host: "{{ vault_beelink_host }}"
  vars:
    ansible_user: beelink
    ansible_become_password: "{{ vault_become_password }}"
    ansible_ssh_private_key_file: ~/.ssh/beelink_ed25519